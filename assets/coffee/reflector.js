// Generated by CoffeeScript 1.6.2
(function() {
  var Reflector;

  Reflector = (function() {
    function Reflector(reflectorType, debug) {
      var log, warn;

      this.reflectorType = reflectorType;
      log = function(text) {
        if (debug && console.log) {
          console.log(text);
        }
      };
      warn = function(text) {
        if (debug && console.warn) {
          console.warn(text);
        }
      };
      this.getOutput = function(input) {
        var pos, result;

        this.input = input;
        pos = reflectorType["in"].indexOf(input);
        result = reflectorType.out.substring(pos, pos + 1);
        return result;
      };
    }

    return Reflector;

  })();

  this.Reflector = Reflector;

}).call(this);
